//QuerySelectorAll
const seleccion = document.querySelectorAll('.card');
console.log(seleccion)

const formularios = document.querySelectorAll('#formulario');
console.log(formularios);

//Si no existe un elemento retorna NodeList Vacio